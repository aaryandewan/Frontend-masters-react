import React, { useState } from "react";

const useDropdown = (label, defaultState, options) => {
  cosnt[(state, setstate)] = useState(defaultState);
  const id = `use-dropdown${label.replace(" ", "").toLowerCase()}`;
};

export default useDropdown;
